Prime Number(1-(user_input))

1. Assign variable with a integer value from user input in variable "n".
2. Check if the input is an integer or not
3. If it is, then check if it is only divisble by one or itself
4. To check, use if num % num == 0 and num % 1 == 0, and check if its not divisble from 2 to n/2, 
5. 



